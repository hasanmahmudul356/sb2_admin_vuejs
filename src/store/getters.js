export const getters = {
    model_title(state) {
        return state.modalTitle;
    },
    formObject(state) {
        return state.formObject;
    },
    formType(state) {
        return state.formType;
    }

}
